declare module 'Firebase' {
    // Provide type declarations for the exports
    export const db: any; // Replace `any` with more specific types if possible
    export const auth: any; // Replace `any` with more specific types if possible
      export const app: any; // Replace `any` with more specific types if possible
  }
  